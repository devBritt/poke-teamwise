<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Poké TeamWise</title>
    {{!-- Foundation CSS --}}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/foundation-sites@6.7.5/dist/css/foundation.min.css" crossorigin="anonymous" />
    {{!-- Fonts --}}
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Anek+Telugu:wght@300;400;500;700&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />
    {{!-- custom styles --}}
    <link href="/stylesheets/styles.css" rel="stylesheet" />
</head>
<body>
    <header>
        <div class="contain-to-grid sticky">
            <nav class="top-bar navbar align-justify" data-topbar role="navigation">
                <a href="/"><h1 class="nav-link" id="nav-title">Poké TeamWise</h1></a>
                <section class="grid-x grid-padding-x align-bottom">
                    <a href="/generator" class="nav-link">Generator</a>
                    {{#if loggedIn}}
                    <a href="/dashboard" class="nav-link">Dashboard</a>
                    <a id="logout" class="nav-link">Logout</a>
                    {{else}}
                    <a href="/login" class="nav-link">Login/Signup</a>
                    {{/if}}
                </section>
            </nav>
        </div>
    </header>
    <main>
        {{{ body }}}
    </main>
    <footer>

    </footer>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
    {{!-- Foundation CSS JS --}}
    <script src="https://cdn.jsdelivr.net/npm/foundation-sites@6.7.5/dist/js/foundation.min.js" crossorigin="anonymous"></script>
</body>
</html>